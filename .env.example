# Copy this file to .env and fill in the values
# .env is gitignored — never commit it

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=payroll_dw

# Spring Boot backend
DB_URL=jdbc:postgresql://postgres:5432/payroll_dw
DB_USER=postgres
DB_PASS=postgres
JWT_SECRET=change-me-to-a-random-256-bit-string
ML_API_URL=http://ml:8000

# Email (Gmail SMTP)
MAIL_USER=asmazehi.insaf@gmail.com
MAIL_PASS=your-gmail-app-password

# Python ML service (reads from PostgreSQL)
PG_HOST=postgres
PG_PORT=5432
PG_DB=payroll_dw
PG_USER=postgres
PG_PASSWORD=postgres
