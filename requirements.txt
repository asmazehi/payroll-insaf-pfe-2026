# INSAF Payroll Intelligence Platform — Python dependencies

# ETL core
chardet>=5.0          # encoding detection (fallback)
ftfy>=6.0             # unicode repair (optional enhancement)

# Indemnity Arabic encoding (indem_def uses different encoding layer)
# No extra deps needed — latin-1 → cp1256 is built-in to Python

# Future: Excel upload support
openpyxl>=3.1

# PostgreSQL
psycopg2-binary>=2.9

# Web platform (Phase 3)
# fastapi>=0.100
# uvicorn>=0.20
# python-multipart>=0.0.6

# Data science (Phase 2)
# pandas>=2.0
# scikit-learn>=1.3
# numpy>=1.24
